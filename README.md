# Домашнее задание №3: Docker + Bash (футбольные стадионы)
Репозиторий домашнего задания 3. Я решил сделать данные про футбольные стадионы в честь чемпионата мира, надеюсь эта категория подходит).

## Команды

- `./run.sh build_generator` — собрать образ генератора  
- `./run.sh run_generator` — сгенерировать `data/data.csv`  
- `./run.sh create_local_data` — локальная генерация `local_data/data.csv` (без Docker)  
- `./run.sh build_reporter` — собрать образ аналитика  
- `./run.sh run_reporter` — сгенерировать `data/report.html`  
- `./run.sh structure` — показать структуру проекта  
- `./run.sh clear_data` — удалить `data/*.csv` и `data/*.html`  
- `./run.sh inside_generator` — показать содержимое `data` изнутри контейнера генератора  
- `./run.sh inside_reporter` — показать содержимое `data` изнутри контейнера аналитика  
- `./run.sh report_server` — запустить веб-сервер (nginx) на порту 8080 для просмотра отчёта

## Как открыть report.html в браузере

1. Создайте образы через `./run.sh build_generator` и `./run.sh build_reporter`.
2. Выполните `./run.sh run_generator` и `./run.sh run_reporter`.
3. Запустите веб-сервер: `./run.sh report_server`.  
4. При локальной работе вставте в браузер http://localhost:8080/report.html иначе в нижней панели Codespaces откройте вкладку **Ports**. (в коде есть комментарий, к сожаления я не смог генератор ссылки довести до ума)
5. Найдите порт `8080`, нажмите на ссылку в столбце **Перенаправленный адрес**.  
6. В адресной строке браузера добавьте в конец `/report.html` и нажмите Enter.
7. Наслаждайтесь статистикой ^_^ (блин это выглядит как будто ии, но это я реально написал строчку... доказательством скажу, что у меня почему-то не работает windows+. поэтому я руками смайлик писал)
